_Note: This is a auto generated page. Do not modify it manually!_

h1. Table of Contents

{toc:printable=true|maxLevel=2|minLevel=2|class=bigpink|exclude=[1//2]|type=list|outline=clear|style=disc|numbering=false|include=.*}


h2. Source
Basis of this metadata extraction is:
{% if isApp %}
- App Version: {{ metaDataBasis.marketingVersion }} {% if metaDataBasis.buildNumber %}({{ metaDataBasis.buildNumber }}){% endif %} {% else %}
- Pod version: {{ metaDataBasis.podVersion }} {% endif %}
- Branch: {{ metaDataBasis.branch }}

h2. General

{{ projectTable }}
CPCL: Comment lines per Code Line

{% if isApp %}{{ atsExceptionsTable }}{% endif %}

{% if idfaAppearances %}
h2. IDFA: Code appearances

|| File |
{% for appearance in idfaAppearances %}| {{ appearance }}|
{% endfor %}{% endif %}

h2. CocoaPods

h4. RSS Feed

GitHub provides a RSS Atom Feed which contains the newest release version of a repository.

Use [^{{ rssFeedFilename }}] to import all the feeds from the used Pods into a RSS Reader App.

The OPML file contains all used Pods which are hosted on GitHub.

{{ noneGithubRepositorySection }}

h4. Used Pods

{{ podsTable }}
